# Packet-Analysis-System
A modular C++ project that simulates a network packet analyzer and intrusion detection system (IDS) using fundamental linear data structures. The system processes packets based on priority, analyzes protocol layers, inspects payloads for suspicious patterns, and assigns risk scores to detect potential threats.
